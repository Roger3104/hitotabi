<% if current_user && current_user.following?(user) && current_user != post.user %>
  <%= link_to users_relationships_path(user_id: user.id, post_id: post.id), method: :delete, class:"link", remote: true, data: {toggle: "tooltip", placement: "right"}, title: "フォロー済み" do%>
    <sapn class="btn-text"><i class="fas fa-user" style="color: black;"></i></span>
  <% end %>
<% elsif current_user && current_user != post.user %>
  <%= link_to users_relationships_path(user_id: user.id, post_id: post.id), method: :post, class:"link", remote: true, data: {toggle: "tooltip", placement: "right"}, title: "フォローする" do %>
    <span class="btn-text"><i class="fas fa-user-plus" style="color: black;"></i></span>
  <% end %>
<% end %>
